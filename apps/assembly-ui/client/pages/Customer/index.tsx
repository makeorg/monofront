import React, { FC } from 'react';

const CustomerPage: FC = () => <div>Customer</div>;

// default export needed for loadable component
export default CustomerPage; // eslint-disable-line import/no-default-export
