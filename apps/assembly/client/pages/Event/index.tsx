import React, { FC } from 'react';

const EventPage: FC = () => <div>Event</div>;

// default export needed for loadable component
export default EventPage; // eslint-disable-line import/no-default-export
