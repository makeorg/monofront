import React, { FC } from 'react';

const PrivacyPolicyPage: FC = () => <div>PrivacyPolicy</div>;

// default export needed for loadable component
export default PrivacyPolicyPage; // eslint-disable-line import/no-default-export
