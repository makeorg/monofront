import React, { FC } from 'react';

const NotFoundPage: FC = () => <div>Not found</div>;

// default export needed for loadable component
export default NotFoundPage; // eslint-disable-line import/no-default-export
